cmake_minimum_required(VERSION 3.14)
project(robot_geometry)
find_package(ament_cmake REQUIRED)
if(BUILD_TESTING)
  find_package(ament_cmake_gtest REQUIRED)
  ament_add_gtest(geometry_tests test/geometry_tests.cpp)
endif()
ament_package()
