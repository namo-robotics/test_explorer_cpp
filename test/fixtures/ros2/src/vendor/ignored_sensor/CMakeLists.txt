cmake_minimum_required(VERSION 3.14)
project(ignored_sensor)
find_package(ament_cmake REQUIRED)
if(BUILD_TESTING)
  find_package(ament_cmake_gtest REQUIRED)
  ament_add_gtest(sensor_tests test/sensor_tests.cpp)
endif()
ament_package()
